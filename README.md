## Project yang akan digunakan sebagai sinkronisasi data, autentikasi, dan authorization dari aplikasi (mobile) left days

tujuan dari project ini
- sinkronisasi data dan menyimpan ke MongoDB database untuk dari aplikasi left days 
- autentikasi untuk mengakses data user menggunakan Google OAuth 2.0 
- authorization untuk request data
